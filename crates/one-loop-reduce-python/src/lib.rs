//! Symbolica-community Python bindings for [`oneloopreduce`].
//!
//! Stub. Phase 2 fills this in with the `SymbolicaCommunityModule`
//! implementation (`pub struct CommunityModule;`) and the `register_module!`
//! wiring that exposes the reducer as the Python module `oneloopreduce`.
